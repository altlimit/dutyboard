{
  "name": "dutyboard",
  "version": "2.2.5",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "DutyBoard \u2014 a coordination bus, external working memory and human-in-the-loop decision engine for autonomous agents, built on altengine functions + datastore + auth + channel.",
  "scripts": {
    "dev": "npm run dev -w app",
    "dev:site": "sitegen -site site -public site/public -serve",
    "build": "npm run build:fn && npm run build:app && npm run build:site",
    "build:fn": "node functions/build.mjs",
    "build:app": "npm run build -w app",
    "build:site": "sitegen -site site -public site/public -minify -clean && node -e \"require('fs').copyFileSync('agent/OPERATING.md','site/public/agent.md')\"",
    "provision": "sh scripts/provision.sh",
    "setup": "npm run build:fn && cd cli && go run ./cmd/dutyboard --provision-only --local --source ..",
    "deploy": "npm run build:fn && npm run build:app && cd cli && go run ./cmd/dutyboard --provision-only --source ..",
    "deploy:site": "node scripts/deploy-site.mjs",
    "demo": "node scripts/demo.mjs",
    "smoke": "node scripts/smoke.mjs",
    "smoke:runner": "node scripts/smoke-runner.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "workspaces": [
    "app"
  ]
}
