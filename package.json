{
  "name": "dutyboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DutyBoard — a live civic-duty board built as a backend-less Vue 3 static site on altengine (auth + datastore + channel).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
