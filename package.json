{
  "name": "dutyboard",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "DutyBoard — a coordination bus, external working memory and human-in-the-loop decision engine for autonomous agents, built on altengine functions + datastore + auth + channel.",
  "scripts": {
    "build:fn": "node functions/build.mjs",
    "deploy": "npm run build:fn && node scripts/deploy.mjs",
    "setup": "node scripts/setup.mjs",
    "smoke": "node scripts/smoke.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "esbuild": "^0.25.0",
    "vite": "^6.0.7"
  }
}
