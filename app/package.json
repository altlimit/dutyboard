{
  "name": "dutyboard-console",
  "private": true,
  "type": "module",
  "description": "The DutyBoard console: a static Vue app the provisioner publishes to each deployment's own static site.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
