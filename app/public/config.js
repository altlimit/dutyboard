// Replaced when the console is deployed next to a DutyBoard (the `dutyboard` provisioner writes
// it). Left null, the console uses what it was built with — see src/config.js.
window.DUTYBOARD_CONFIG = null;
